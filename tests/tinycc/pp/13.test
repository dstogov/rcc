--TEST--
TCC 13
--ARGS--
-E -P
--XFAIL--
preprocessing of assembler is not supported yet
--CODE--
# `modelist' label. Each video mode record looks like:
#ifdef AAA
# modelist' label. Each video mode record looks like:
#endif
.text
endtext:
--EXPECT--
.text
endtext:
